#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
// def fibonacci(n): (unrecognized line)
// fib = [0, 1] (unrecognized line)
std::vector<int> fib(n) {
while (len(fib) < n) {
fib.push_back(fib[-1] + fib[-2]);
return fib



int main() {

    return 0;
}